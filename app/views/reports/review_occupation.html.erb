<header>
  <h2 class="heading-xlarge">Work you could do</h2>
</header>

<p class="lede">Use this information to search online, tailor your CV and plan your career.</p>

<% occupation = @report.occupations_to_review.first %>
<section>
  <div class="job_item">
    <header>
      <h2 class="heading-large"><%= occupation.title %></h2>
    </header>

    <p><%= occupation.description %></p>

    <div class="grid-row">
      <div class="column-half">
        <h3 class="heading-small">Typical pay and hours</h3>
        <ul>
          <li>Normal hours worked per week: <%= occupation.week_hours %></li>
          <li>Estimated weekly pay: £<%= occupation.week_pay %></li>
        </ul>
      </div>
      <div class="column-half">
        <h3 class="heading-small">Typical qualifications required</h3>
        <p><%= occupation.qualifications %></p>
      </div>
    </div>

    <div class="grid-row">
      <div class="column-half">
       <h3 class="heading-small">Typical tasks</h3>
        <ul class="list-bullet">
          <% occupation.formatted_tasks.each do |task| %>
            <li><%=task%></li>
          <% end %>
        </ul>
      </div>

      <div class="column-half">
        <h3 class="heading-small">Useful search terms</h3>
        <em><%= occupation.additional_titles %></em>
      </div>
    </div>
  </div>
  <hr>
</section>

<%= form_for([ @report, occupation]) do |f| %>
  <a id="show-accept" href="#show-accept" class="button hide-if-accept">Accept</a>
  <%= submit_tag "Accept", class: "button show-if-accept" %>
  <%= submit_tag "Refuse", class: "button" %>
  <div class="show-if-accept"</div>
    <% @actions.each do |action, details| %>
      <label class="block-label">
        <%= check_box_tag "action_#{action}", nil, false, "title": "Select action 1" %>
        <h4 class="bold-small"><%=details["title"]%></h4>
        <p><%=details["description"]%></p>
      </label>
    <% end %>
  </div>
<% end %>
