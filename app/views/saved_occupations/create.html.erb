<div id="success" class="information-box">
  <p>
    "<%= @occupation.title %>" saved.
  </p>
</div>

<p><%= link_to("Return to #{@occupation.title}", [current_scrapbook, @occupation]) %></p>
<p><a href="<%= scrapbook_search_path(query: @query) %>" id="back-to-search-results">Back to search results</a></p>
