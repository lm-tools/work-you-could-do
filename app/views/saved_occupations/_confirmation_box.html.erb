<div id="success" class="information-box">
  <p aria-live="polite" role="alert">
    &lsquo;<%= occupation.title %>&rsquo; saved.
  </p>
  <p>
    <%= link_to("Saved #{pluralize_without_count(current_scrapbook.saved_occupations.size, "role")}", current_scrapbook) %>
  </p>
</div>
