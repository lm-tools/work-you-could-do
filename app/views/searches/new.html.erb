<% content_for(:page_title_prefix, "Find information on roles") %>

<% content_for :breadcrumbs do %>
<ol>
  <li><a href="<%= scrapbook_introduction_path(current_scrapbook) %>">Introduction</a></li>
  <li>Search</li>
</ol>
<% end %>

<h1 class="heading-xlarge">Find information on roles</h1>

<% if current_scrapbook.saved_occupations.any? %>
  <div id="success" class="information-box">
    <p>You have <%= link_to("#{pluralize(current_scrapbook.saved_occupations.size, "saved role")}", current_scrapbook) %>.</p>
  </div>
<% end %>

<%= render "searches/form", show_hints: true %>
