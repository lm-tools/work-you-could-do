<header>
  <h2 class="heading-xlarge">Your work search activities</h2>
</header>

<% @scrapbook.occupations.each do |occupation| %>
  <section id="<%=occupation.id%>">
    <%= render "occupations/details", occupation: occupation %>
    <hr>
  </section>
<% end %>

<footer class="TBC">
  <a href="<%=root_url%>" class="link-back">start again</a>
</footer>
