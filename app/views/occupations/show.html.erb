<%= form_tag(scrapbook_saved_occupations_path, method: "POST", class: "save-occupation-form") do |f| %>
  <input type="hidden" name="occupation_id" value="<%= @occupation.id %>">
  <input type="submit" id="save-occupation"  class="button" value="Save to my list">
<% end %>
<a href="<%= scrapbook_search_path(query: @query) %>" id="back-to-search-results">Back to search results</a>
<%= render "occupations/details", occupation: @occupation %>
